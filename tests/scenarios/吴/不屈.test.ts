import { describe, expect, it } from 'vitest';
import { scenario } from '../../scenario-runner';

describe('周泰 - 不屈', () => {
  it.skip('不屈：濒死时翻牌作为创牌判定（需要创牌机制支持）', () => {
    // 不屈需要"创"牌系统：在武将牌上放置标记牌、比较点数
    // 涉及自定义濒死处理流程，当前引擎基础设施不支持
  });
});
