import { describe, expect, it } from 'vitest';

describe('张昭张纮 - 固政', () => {
  it.skip('固政：其他角色弃牌阶段回收牌（需要弃牌阶段拦截机制）', () => {
    // 固政需要在其他角色的弃牌阶段结束时拦截弃牌堆
    // 涉及跨角色弃牌阶段监控和弃牌堆操作，当前引擎基础设施不支持
  });
});
