import { describe, expect, it } from 'vitest';
import { scenario } from '../../scenario-runner';

describe('太史慈 - 天义', () => {
  it.skip('天义：拼点赢后攻击范围无限+额外杀（需要拼点机制支持）', () => {
    // 天义需要拼点系统：双方各出一张手牌比点数
    // 涉及交互式双玩家选择，当前引擎基础设施不支持
  });
});
