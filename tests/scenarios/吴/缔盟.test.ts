import { describe, expect, it } from 'vitest';
import { scenario } from '../../scenario-runner';

describe('鲁肃 - 缔盟', () => {
  it.skip('缔盟：交换两名角色手牌（需要手牌交换机制支持）', () => {
    // 缔盟需要选择两名目标角色，弃置差值牌数后交换手牌
    // 涉及双目标选择和手牌批量交换，当前引擎基础设施不支持
  });
});
