# 界陆逊

> **官方系列**：`界限突破`　|　**来源**：[三国杀OL hero/310](https://www.sanguosha.com/hero/310)　|　**引擎状态**：✅已实现

![界陆逊](../.hero-cache/images/310.png)

## 基本信息
| 属性 | 值 |
|------|-----|
| 名称 | 界陆逊 |
| 势力 | 吴 |
| 体力上限 | 3 |
| 性别 | 男 |
| 官方系列 | 界限突破 |

## 技能列表
| 技能名称 | 类型 | 描述（官方逐字） |
|----------|------|------------------|
| 界谦逊 | 被动技 | 当一张延时锦囊牌或其他角色使用的普通锦囊牌对你生效时，若你为唯一目标，你可以将所有手牌移出游戏直到回合结束。 |
| 界连营 | 被动技 | 当你失去所有手牌后，你可以令至多X名角色各摸一张牌（X为你失去的手牌数）。 |

## 技能详解

### 界谦逊（被动技）

> **机制简述**：延时或普通锦囊对你生效且唯一目标时手牌移出

**官方效果**：当一张延时锦囊牌或其他角色使用的普通锦囊牌对你生效时，若你为唯一目标，你可以将所有手牌移出游戏直到回合结束。

**实现要点**：
- 模式 A 触发型，两条触发钩点：
  - **「添加延时锦囊」after**：延时锦囊（乐不思蜀/兵粮寸断/闪电）放置到陆逊判定区时触发（延时锦囊天然单目标）。
  - **「成为目标」after**：他人使用的普通锦囊以陆逊为唯一目标时触发。判定：顶帧 `from≠自己`、顶帧处理区有「普通锦囊」牌（`type==='锦囊牌'` 且非延时/响应）、`frame.params.targets` 恰好 1 且为自己。
- 询问 confirm；确认后用「移出游戏」atom 把全部手牌暂存到 `player.vars['界谦逊/移出']`。
- 「回合结束」after-hook：检测到移出区非空 → 用「归还移出牌」atom 归还手牌。
- **联动**：移出全部手牌会使手牌归零 → 触发「界连营」，X = 失去的手牌数 = 移出张数。这是界陆逊的核心 combo（谦逊搬空手牌 → 连营令多名角色摸牌）。

**实现差异/边界**：
- **直接生效型普通锦囊未覆盖**：顺手牵羊/过河拆桥/火攻 等直接生效型普通锦囊不走「成为目标」atom，本实现未覆盖。引擎无统一「锦囊生效」钩点，逐牌挂获得/弃置 before-hook 会破坏这些锦囊的选牌/结算流程，风险高于收益，留作后续工作。当前覆盖：全部延时锦囊 + 经「成为目标」的单目标普通锦囊（决斗，及未来任何走 成为目标 流程的单目标普通锦囊）。

### 界连营（被动技）

> **机制简述**：失去所有手牌后令至多X名角色各摸一牌

**官方效果**：当你失去所有手牌后，你可以令至多X名角色各摸一张牌（X为你失去的手牌数）。

**实现要点**：
- 模式 A（被动触发）：after hook 挂载点覆盖所有"手牌减少"的路径——移动牌/给予/装备/移出游戏 after + 获得/弃置 before+after。
- 触发条件：本次 atom 使自己手牌区牌减少，且 apply 后手牌数 == 0（失去所有手牌）。
- "你可以" → 询问 confirm(trigger)；确认后用 `choosePlayer(min=1,max=X)` 选目标，对每个目标各「摸牌」(count=1)。
- **X 计数机制**：每个 before-hook 在涉及界陆逊手牌时写入 `HAND_BEFORE_KEY = apply 前的手牌数`；对应 after-hook 读取并删除，作为 X 传给 `maybeTriggerLianYing`。
  - 移出游戏（界谦逊）一次移走 N 张整手牌 → before 记 N、after 手牌归零 → X=N（联动核心）。
  - 移动牌/给予/装备 单张移走 → X=1（失去最后一张的边界）。
  - 获得/弃置 可能作用于装备/判定区（非手牌），before-hook 仅在涉及手牌时写入。
- "角色"含自己（filter 仅校验存活）；"至多 X 名"是硬规则：max 在引擎侧 clamp（防越权客户端多选）；摸牌不减少手牌不会自递归；牌堆+弃牌堆皆空 → 无法摸牌跳过询问。

**实现差异/边界**：实现与官方描述一致。X 定义为"本次使手牌归零的那次失去事件中失去的手牌张数"（OL 现行版语义，不是早期版本的体力值）。

**与标版差异**：标版谦逊为锁定技（免疫顺手牵羊/乐不思蜀）；OL 界谦逊是全新的触发型可选技（移出手牌+回合结束归还），不免疫任何锦囊——二者机制完全不同，引擎新建 `界谦逊.ts`，标版保留不动。标版连营"失去最后手牌后自己摸一张"；界版改为"令至多 X 名角色各摸一张"（X=失去的手牌数）。
